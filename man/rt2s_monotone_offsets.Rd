% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequencies.R
\name{rt2s_monotone_offsets}
\alias{rt2s_monotone_offsets}
\title{Make Stop-Time Offsets Monotone}
\usage{
rt2s_monotone_offsets(travel, dwell)
}
\arguments{
\item{travel}{Numeric vector of arrival offsets from trip start, in seconds.}

\item{dwell}{Numeric vector of dwell offsets, in seconds, with the same
length as \code{travel}.}
}
\value{
A list with integer vectors \code{arrival} and \code{departure}. Each
departure is its arrival plus its dwell, and each arrival is at least the
previous departure.
}
\description{
Rounds travel and dwell offsets to integer seconds, clamps negative values
to zero, and makes a stop pattern monotone with a forward pass.
}
\details{
Inputs must be finite numeric vectors of equal length. Values are
rounded with \code{\link[base:round]{base::round()}} before negative values are clamped to zero.
For each stop, the returned arrival is the larger of its clamped travel
offset and the previous returned departure. This satisfies GTFS along-trip
monotonicity even when independently estimated offsets are out of order.
}
\examples{
rt2s_monotone_offsets(
  travel = c(-2.4, 300.6, 298.2),
  dwell = c(0, 30.6, 15)
)
}
